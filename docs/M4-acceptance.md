# M4 验收记录：网关模式 + 流量护栏 + 回滚演练

执行与证据时间：2026-09-19 23:0x–23:2x　|　全部为实测原始输出

## 1. 网关模式（FR3）

### 1.1 交付物
`scripts/gateway-mode.sh` —— 幂等脚本，动作与保证：

| 动作 | 说明 |
|---|---|
| 记录原值 | 首次 apply 时把 `ip_forward`、FORWARD 默认策略写入 `/etc/kelp/gateway-originals.json`，供回滚精确还原 |
| 开启转发 | `sysctl net.ipv4.ip_forward=1` + 落 `/etc/sysctl.d/99-kelp-gateway.conf`（重启仍生效） |
| FORWARD 放行 | 仅针对 `ens160 ↔ tun0`（+ 自测 veth）插入带 `KELP-GATEWAY` 注释的 ACCEPT 规则；用 `iptables -C` 判重，重复执行不产生重复规则 |
| 开机自恢复 | `kelp-gateway.service`（oneshot）开机重放规则 |
| 自测 | 用**网络命名空间**模拟一台【未装 EasyTier 客户端】的设备（IP 192.168.50.241/24，网关指向本机），测试访问 B 站点 `https://192.168.1.99:5667/` |

### 1.2 执行方式（需 root，在 192.168.50.9 上）
```bash
sudo bash /tmp/kelp-gateway.sh apply      # 开启 + 前后自测
sudo bash /tmp/kelp-gateway.sh status     # 只读查看
sudo bash /tmp/kelp-gateway.sh rollback   # 回滚（恢复 ip_forward 原值 + 删规则）
```

### 1.3 验收证据
- [ ] **待用户执行**（唯一需要用户动手的一步）：执行上面第一条命令后，把输出贴回；预期形态为
      「开启前自测 ping 不通 / HTTP 000 → 开启后自测 ping 通 / HTTP 200」。
- [ ] 之后由助手补齐：`iptables -S FORWARD`、`ip_forward` 值、以及从 50.9 视角的免客户端访问证明。
- [x] 脚本本机与 50.9 双侧 `bash -n` 语法校验通过（2026-09-19 23:1x）。
- [x] 回滚路径已按 `docs/rollback.md` §1.3 定义（记录原值 + 精确删除 + 恢复开关）。

### 1.4 客户端配置说明（免客户端设备怎么用）
| 端 | 做法 | 代价 |
|---|---|---|
| 首选：静态路由（FR3 第 1 条） | 只加一条目标网段路由，例如 macOS `sudo route add -net 192.168.1.0/24 192.168.50.9`；Windows `route add 192.168.1.0 mask 255.255.255.0 192.168.50.9`；Linux `sudo ip route add 192.168.1.0/24 via 192.168.50.9` | 公网访问路径**完全不变**，无额外延迟与流量费 |
| 次选：改默认网关（FR3 第 2 条） | 设备无法加路由时，把默认网关设为 192.168.50.9（如 AppleTV/部分电视） | 该设备**全部流量**多一跳；若走到中继则按 0.8 元/GB 计费。铁律 6.7 要求逐设备确认 |
| DNS | **默认不改** | 仅 iOS/AppleTV 被“iCloud 私密代理/私密 DNS”拦住组网地址时，才把该设备 DNS 改为 `223.5.5.5`/`114.114.114.114`（副作用：公司内网域名、路由器本地主机名、路由器 DNS 过滤失效） |

## 2. 流量护栏（FR7）

自建实现（不依赖阿里云控制台），落在只读面板里：

- 数据源：公网节点 `/proc/net/dev` 中所有非 `lo` 接口的**发出字节**（阿里云按出网流量计费，故只看 egress）
- 统计：今日 / 本月累计，及按 0.8 元/GB 的预估费用；阈值默认 **5 GB/日**（可配）
- 告警：超阈值时面板顶部弹**红色横幅**，文案含“今日用量 / 阈值 / 预估费用 / 请检查是否有设备在走中继”
- 持久化：`/var/lib/kelp/traffic.json`（跨重启保留当日基线）

### 2.1 触发一次测试告警（验收要求）
把阈值临时改为 `0.00001 GB`（10 kB，小于当日已用量），重启面板后实测：

```
alert= True | 今日 132.72 kB | 阈值 1e-05 GB/日 | 预估 ¥ 0.0
```

面板顶部横幅实测文案：

> 🔴 **今日出网流量已超阈值** —— 今日 282.42 kB / 阈值 1e-05 GB/日，预估 ¥0（0.8 元/GB）。请检查是否有设备在走中继。

证据图：`docs/evidence/M4-traffic-alert.png`

随后恢复正式阈值并复核：`恢复后 alert= False | 阈值 5 GB/日 | 今日 388.32 kB`

## 3. 回滚演练（FR8）

按 `docs/rollback.md` 逐项实测（详见该文档 §4 汇总表）：

| 演练 | 原始输出要点 | 结论 |
|---|---|---|
| 停 B 站点容器 `docker stop kelp-b-node` | 65 秒内面板：`飞牛 NAS OFFLINE · 最后在线 23:07:45`；另 2 节点仍 `p2p` | 只影响该站点，离线判定与最后在线时间工作正常 ✅ |
| 重启 B 站点容器 | 35 秒内 `online p2p 15.11ms`，汇总回到 `3 在线 / 3 P2P / 0 中继` | 自动恢复 ✅ |
| 停只读面板 `systemctl stop kelp-panel` | 期间从 B 站点访问 `https://192.168.50.9/` 仍 **401**，peer 数仍是 1 条 p2p | 面板与数据面解耦 ✅ |
| 停公网节点 `systemctl stop easytier` | **3 秒内** A↔B 全部不通（`http=000`、`192.168.50.9:22 closed`）；重启后 15 秒恢复 2 条 P2P | ⚠️ 见 §4：公网节点是**硬单点** |

## 4. 重要实测更正：公网节点是硬单点（原假设被推翻）

任务书风险表原写“组网本身可 P2P，节点只是信令；节点宕机时已建立的连接仍可用”。**实测推翻**：

```
hub 停机 +0s : 邻居表中已无 kelp-50-9 ; https://192.168.50.9/ -> http=000
hub 停机 +3s : 同上
hub 停机 +8/15/25/40s : 同上（一直不通）
恢复 hub 后  : 15 秒内 2 条 P2P 自动重建
```

结论：**公网节点停止服务 → 整个组网立即中断**（含已在 P2P 直连的两台设备之间的流量），恢复后自动重连。影响：公网节点不可用期间，所有跨站点访问中断、面板不可用。

缓解选项（待用户决定，见任务书 D21）：
1. 接受（阿里云 ECS 稳定性高、故障自恢复；组网本身数据不经公网节点，恢复后不产生额外流量费）
2. 增加**第二个公网节点**（另一台 VPS，或异地低配实例）做信令冗余——成本最低的可靠性提升
3. 让节点额外连接 EasyTier 社区公共服务器作为备用信令——**需把网络名/密钥交给第三方**，隐私权衡，原则上不做

## 5. 副作用验收（用户最关心的“公网访问不受影响”）

本机（macOS）在本次所有变更**前后**的基础网络基线（只读采集）：

| 项 | 值 | 说明 |
|---|---|---|
| 默认网关 | `192.168.50.1`（接口 en0） | **未被任何组网配置改动**（铁律 6.7） |
| 公网出口 IP | `36.151.107.250` | 与部署前一致 |
| 延迟 | 223.5.5.5 `19.6ms` / www.baidu.com `10.3ms` / 公网节点 `18.0ms` | 正常水平 |
| 下载速率 | 清华源前 20MB：`2.38 MB/s`（≈19 Mbps） | 家宽正常水平 |

补充说明：本次 M4 的转发开关只开在 192.168.50.9 的**转发路径**上，且自测用的是**虚拟设备**（网络命名空间），不改任何真实客户端的路由或网关 → 公网访问路径与速率**按构造即不受影响**；M4 完成后需再采一次同样基线做对照（见“遗留”）。

## 6. 遗留

- [ ] 用户执行 `sudo bash /tmp/kelp-gateway.sh apply` → 补齐 §1.3 证据
- [ ] 变更后再采一次 §5 基线做前后对照
- [ ] M3 期间补：iOS/AppleTV 侧 DNS/私密代理坑的**真机**验证（任务书 M4 验收第 2 条）
- [ ] 50.9 与 hub 补 `--need-p2p`；50.9 RPC 白名单补 `127.0.0.1`（可与网关脚本同一次 sudo 窗口一起做，已合并到交付命令）
