# 海带 (Kelp) 项目任务书

- 文档版本: v1.1（2026-09-19 补充：FR9 统一查看界面、FR3 接入优先序、铁律 6.7、M1/M2/M4 验收项、D9–D11）
- 日期: 2026-09-19
- 状态: **待用户审核**；用户明确指令"开始"后方可动手（铁律 §6.1）
- 调用说明: 本文件是项目唯一权威任务书。任何后续 Agent 接手本项目，必须先读本文件，再读同目录 `checkpoint.md`，遵守其中全部铁律与决策记录，再开始工作。
- 凭据说明: 所有服务器凭据（含 47.116.73.216 的 root 口令）**不写入本文件、不写入仓库、不写入任何日志**，仅在部署会话中以交互方式使用（见铁律 §6.6）。

## 1. 项目基本信息

| 项 | 值 |
|---|---|
| 中文名 | 海带 |
| 英文名 | Kelp |
| 定位 | 自建的异地组网 + 远程访问系统：替代商业产品"节点小宝"的免费版限制，实现多站点内网互通、无客户端设备网关接入、手机外网接入；**仅个人自用，不公开分发** |
| 目标环境 | 阿里云 ECS(公网节点) + 家庭 macOS/Linux + NAS(fnOS/Docker) + Android/iOS |
| 技术栈 | EasyTier（Apache-2.0，Rust，去中心化异地组网）+ 系统原生工具（systemd / launchd / Docker / ip_forward / nftables）；核心不自研代码 |
| 项目目录 | /Users/winann/01-project/07-kelp |
| 公网节点 | 47.116.73.216（阿里云华东·上海，AS37963，SSH 用户 root） |
| 成本约束 | VPS 已有；**ECS 按流量计费 0.8 元/GB（出网）** → 决定"节点只做信令与兜底" |

## 2. 背景与问题

用户在 macOS / NAS / 手机上使用商业软件**节点小宝**（本机实测 v4.3.6）做异地组网与远程访问，其免费版限制已影响使用，付费版 30 元/月：

| 项目 | 节点小宝免费版（官方 + 三方实测口径） | 付费版（30 元/月） |
|---|---|---|
| 组网设备 | 可绑定 5 台，其中 **2 台**可开组网 | 5 台 |
| 内网穿透 | 5GB/月，2Mbps，1 个二级域名(80/443)，仅 HTTP/HTTPS | 200GB/月，12Mbps，4 个域名 |
| 组网传输 | P2P 直连不限速不限流量 | 同 |
| 域名 | 只能用 `*.iepose.cn`，不支持自有域名 | 同 |
| 自定义 IP | 不支持 | 支持 |

用户的环境是两个站点 + 移动办公：

- A 站点（家）：`192.168.50.0/24` —— 本机 Mac(50.10)、服务器 50.9(Ubuntu)、电视/AppleTV
- B 站点（NAS 侧）：`192.168.1.0/24` —— 飞牛 NAS(1.99) 等（实测 2026-09-19 从 A 站点不可达、无路由、`~/nas-nfs` 未挂载）
- 移动端：手机在外网访问两站点资源

用户明确需要的核心能力是**"网关"模式**：一台设备当网关，**同局域网内不装客户端的设备**（AppleTV、电视、公司电脑）改一下网关地址即可访问异地网——这正是节点小宝「小宝网关」的做法（官方支持 macOS/Windows/Android/iOS/AppleTV/AndroidTV）。

## 3. 可行性评估结论（实测支撑）

### 3.1 已实测的环境事实（2026-09-19）

| 项目 | 实测结果 |
|---|---|
| 节点小宝实现方式 | 主程序 `nblink` + 网络扩展 `VPNPacket.appex` + `NodeBabyLinkBackup`/`Rfile` 守护进程；虚拟网卡 `utun7` = `10.222.222.1`；已有对端 `100.66.1.1/.3/.4`（均可达）→ 本质是覆盖网络(mesh) + P2P |
| 47.116.73.216 | ICMP 通（家→它 19.7ms，3 跳）；**仅 22 端口开放**；`SSH-2.0-OpenSSH_10.2p1 Ubuntu-2ubuntu3.6`（较新 Ubuntu，机器干净） |
| 家宽出口 | 公网 IPv4 `36.151.107.250`（traceroute 第二跳 36.151.107.249，相邻 /30，非 CGNAT） |
| IPv6 | Mac 侧仅 `::1`，50.9 亦无全局 IPv6 → **IPv6 方案不可用** |
| 路由器 | 爱快 `192.168.50.1` 在跑（iKuai 特征响应头），但 `admin/Abc@123456` 登录被拒（10001） |
| 本机现有工具 | 未安装 tailscale / zerotier / frp / cloudflared / wireguard（无历史包袱） |

### 3.2 两条来自云厂商的硬约束

1. **未备案**：`winann.com` 未备案。工信部/阿里云会对"未备案域名解析到境内 ECS"实施网络层拦截——80/443 被拦（HTTP 返回备案提示页；HTTPS 依 SNI 明文检测直接 RST），2025 年起部分厂商扩大到非标端口。
   → **本项目不做公网域名 Web 入口**，所有访问一律走组网隧道（直连内网 IP / 组网 IP）。
2. **按流量计费**：ECS 出网 0.8 元/GB（入网免费）。参照系：100GB/月 = 80 元/月，**高于节点小宝付费版 30 元/月**。
   → **公网节点只承担信令交换与打洞失败时的兜底中继；常态必须是 P2P 直连。**

### 3.3 技术对位（节点小宝 → EasyTier）

| 节点小宝 | EasyTier 对应能力（官方文档） |
|---|---|
| 设备组网（mesh） | 去中心化组网，`--network-name` + `--network-secret` 建网 |
| 全网映射 / 自定义组网 | **子网代理**（点对网，支持网段映射） |
| 小宝网关（本机当网关） | **网对网**：节点 `-n 192.168.50.0/24` 代理本网段 + `ip_forward` + FORWARD 放行，局域网设备把网关指向该机 |
| 手机/AppleTV 免客户端 | **WireGuard 接入**：节点可作 WG 服务端并一键生成客户端配置，导入 iOS/Android 原生 WireGuard App |
| 中心调度服务 | **自建公共节点**：`easytier-core --private-mode true --network-name X`（仅本网络可连） |
| 自定义 IP | `-i 10.144.144.x`（免费） |
| P2P 直连不限速 | UDP NAT 穿透（支持 NAT4-NAT4），失败自动降级中继 |

### 3.4 总体结论

**完全可行，复杂度低-中，自动化程度高（核心组件为现成开源二进制，不自研代码）。** 预计 1~2 天工作量，分 5 个里程碑。唯一真实风险是"打洞失败导致中继流量计费"，已由 §5-FR7（流量护栏）与 §9-M2 验收（必须证明 P2P 直连）覆盖。

## 4. 范围

### 4.1 v1 范围内（已与用户确认，8 项）

- 必做 5 项：公网私有节点、安全组放行、两站点组网 + 子网代理、手机接入、开机自启
- 顺手 3 项：网关模式、固定组网 IP、出网流量告警

### 4.2 v1 范围外（明确不做，原因见下）

| 不做项 | 原因 |
|---|---|
| 公网域名 Web 入口（Cloudflare Tunnel / nginx 反代 / 证书 / DDNS） | 未备案被拦；且走公网入口=VPS 流量=花钱 |
| 自建 RustDesk 中继、Immich 相册、门户页、流量统计限速 | 非必要（远程桌面直连组网 IP 即可；相册用飞牛自带）；精简指令（D5） |
| ACL / 多网络 / 团队网络 | 单人自用不需要，真有多人再立项 |
| P2P 打洞参数调优 | 有公网节点兜底，先跑起来，出问题再调 |
| 统一客户端 App | 技术上不可达节点小宝体验，接受多点几下（不再讨论） |
| 修改爱快路由配置 / 改动节点小宝 | 见铁律 §6.1、§6.2 |

## 5. 功能需求

**FR1 公网私有节点**
VPS 运行 EasyTier 作为本网络专用节点：`--private-mode true`（仅允许本项目网络名+密钥的节点接入）、固定组网 IP、systemd 常驻（`Restart=always`、`LimitNOFILE=1048576`）。职责：信令交换、打洞协助、失败兜底中继。

**FR2 两站点组网 + 子网代理**
A 站点节点代理 `192.168.50.0/24`，B 站点节点代理 `192.168.1.0/24`，实现整段互访（不需要在每台设备装客户端）。站点内节点优先部署在常开设备（50.9 / NAS）。

**FR3 网关模式（用户核心诉求）**
在 Linux 侧节点（50.9）开启转发（`net.ipv4.ip_forward=1` + FORWARD 放行），使局域网内**不装客户端**的设备可访问异地网。

**接入方式优先序（2026-09-19 用户确认副作用后锁定）**：
1. **首选：静态路由**——在客户端只加一条目标网段路由（Windows `route add`、macOS `route add -net`、Linux `ip route add`），**默认网关保持不变**。效果：仅"去往异地网段"的流量走海带，**公网访问路径完全不变**，无额外流量费。
2. **次选：改默认网关**——仅当客户端无法加静态路由时使用（如 AppleTV/电视无该能力）。此时该设备**全部流量**经网关机转发，须评估额外延迟与（可能的）中继流量费。
3. **DNS：默认不改**。组网地址用字面 IP 访问，原理上不需要 DNS。仅在 M4 实测发现 iOS/AppleTV 因 iCloud 私密代理/私密 DNS 拦截组网 IP 时，才对该设备单独把 DNS 改为 `223.5.5.5`/`114.114.114.114`（国内公共 DNS，**不影响公网访问**；副作用仅限：公司内网域名解析、路由器本地主机名、路由器自带 DNS 过滤失效——见 §9）。

交付物含各端配置说明（Windows/macOS/Android/iOS/AppleTV/AndroidTV），并写明上述取舍与"改默认网关"的代价。

**FR4 手机接入**
Android 装 EasyTier 原生 App；iOS 导入节点生成的 WireGuard 门户配置（原生 WireGuard App）。验收需在外网（蜂窝数据）实测访问两站点资源。

**FR5 固定组网 IP 规划**
`10.144.144.0/24`：`.1` = VPS 节点，`.9` = 50.9，`.10` = Mac，`.20` = B 站点节点，`.30+` = 移动端。全部通过 `-i` 显式固定，禁止依赖随机分配。

**FR6 开机自启与常驻**
VPS/50.9 → systemd；Mac → launchd（或 EasyTier GUI 的"注册服务"）；NAS → Docker（`--privileged` + `/dev/net/tun`）。全部须在重启后自动恢复。

**FR7 流量护栏（成本红线）**
1. 常态必须 P2P 直连；M2 验收必须出示 `easytier-cli peer` 的 p2p 证据
2. 阿里云监控配置出网流量告警（建议阈值 ≤5GB/日）
3. 文档写明"中继 = 花钱 = 异常"的判据与排查顺序（NAT 类型 / UDP 放行 / 防火墙）

**FR8 可回滚与卸载**
每个新增组件必须提供停止与卸载命令，并附回滚手册（`docs/rollback.md`）。验收时演练一次"停服务 → 网络恢复到初始状态 → 重新启用"。

**FR9 统一查看界面（设备列表：组网 IP / 在线离线）**
用户要求在一个界面看到"已联网设备 + IP + 在线/离线状态"。EasyTier 是**去中心化**网络，天然没有权威设备名册，故给出三条路径，M1 实测后定稿：

| 路径 | 形态 | 能看到 | 代价 |
|---|---|---|---|
| A. 自建 Web 控制台 | `easytier-web-embed`（Docker，默认 `11211/TCP`；配置下发端 `22020/UDP`），部署在 **50.9**，仅 overlay/内网可达（**不开放公网端口**） | 受管节点的状态、配置、日志 | 节点需以 `-w <用户名>` 启动；引导依赖本机配置文件 |
| B. 官方托管控制台 | `easytier.cn/web`，节点加 `-w <用户名>` | 同上，零运维 | 配置数据经第三方服务器（隐私权衡） |
| C. 只读面板（自建） | 轻量脚本轮询各节点 `easytier-cli -o json`（或 RPC），输出静态页/JSON，挂 50.9 nginx | 组网 IP、hostname、延迟、丢包、rx/tx、**P2P 还是中继**、NAT 类型；配"期望名册"可判**在线/离线 + 最后在线时间** | 只读、不下发配置；需覆盖内网才可访问 |

- 必须写进交付文档的限制：**只有以 `-w` 纳入管理的节点会出现在 Web 面板里**（官方 FAQ 原文）；未纳管的节点只能在各自机器上 `easytier-cli peer` 查看，或由路径 C 汇总。
- 离线判定的实现：维护"期望设备名册"（配置文件，入库），与实时 peer 列表比对 → 未出现即判离线并记录最后在线时间。
- macOS 零成本查看：EasyTier GUI（官方 dmg）直接显示 peer 列表（IP/延迟/收发量）。

## 6. 铁律（违反即验收不通过）

**6.1 未获授权不动手**
用户明确指令"开始"之前，禁止对 VPS、路由器、NAS、本机任何服务执行变更操作。允许的只有只读勘测（且仅在本机侧）。本任务书本身即为待审核产物。

**6.2 不碰任何既有服务**
- 本机（AGENTS.md 铁律）：禁止 kill / unload LM Studio、`com.local.lmstudio-text-shim` 垫片、节点小宝及其守护进程、任何推理进程；禁止 `lms unload --all`；禁止退出/重启 LM Studio
- 禁止修改爱快现有配置、端口映射、DHCP
- 禁止卸载/关闭节点小宝（本项目与其**并存**，虚拟网卡互不冲突：utun 各自独立）
- NAS：只做新增部署，不动现有服务与网络配置

**6.3 最小暴露**
VPS 安全组只放行必需端口（v1 仅 `11010 TCP+UDP`；若 M3 确认用 VPS 做 WG 服务端，再按最小必要追加 `11013/UDP`）。禁止开启 0.0.0.0/0 的无关端口，禁止 DMZ。

**6.4 单一职责、零污染**
新增组件相互独立、可单独停止；不改动系统级网络默认配置（除 FR3 明确要求的 `ip_forward`，且必须记录原值以便还原）。

**6.5 影响面大的操作先确认**
安全组变更、系统转发开关、DNS/路由调整、任何涉及"恢复出厂/重置"类操作，先给出最小可逆步骤并等用户确认。

**6.6 凭据纪律**
服务器凭据不得写入仓库、文档、日志、commit message。部署时以交互方式或临时环境变量传入；会话结束后不在文件中残留。

**6.7 不改客户端默认网关**
禁止把任何设备的默认网关改为组网节点（含电视、AppleTV、办公设备），除非该设备确实无法配置静态路由且用户单独确认。理由：改默认网关会让该设备**全部流量**绕行，可能带来额外延迟与中继流量费（FR3 已锁定优先序）。DNS 同理：默认不动，仅在 FR3 第 3 条情形下按设备单独调整并记录。

## 7. 架构与技术决策

```
   【A 站点 家 192.168.50.0/24】                  【公网节点 47.116.73.216】
    Mac(50.10, 10.144.144.10) ──┐                  EasyTier 私有节点
    50.9(10.144.144.9)  ────────┼── EasyTier ──►  10.144.144.1 :11010 TCP+UDP
    电视/AppleTV（不装客户端）  ─┘   子网代理 50 段   职责：信令 + 打洞协助 + 兜底中继
      ↑ 网关指向 50.9                                   ↑ 可作 WireGuard 服务端（手机）
                                                              ↑ 出网计费 0.8 元/GB
   【B 站点 NAS 侧 192.168.1.0/24】
    NAS / 其他设备 ── EasyTier 节点(10.144.144.20) ── 子网代理 1 段
```

| 组件 | 部署位置 | 关键参数 |
|---|---|---|
| 公网节点 | 47.116.73.216 | `--private-mode true --network-name <X> --network-secret <Y> -i 10.144.144.1`；systemd 常驻 |
| A 站点节点 | 50.9（常开）+ Mac | `-i 10.144.144.9/10 -n 192.168.50.0/24`；50.9 兼网关（`ip_forward=1`） |
| B 站点节点 | NAS 或该侧常开设备 | `-i 10.144.144.20 -n 192.168.1.0/24` |
| 移动端 | 手机 | Android 原生 App / iOS WireGuard 门户配置 |
| 网络标识 | — | 网络名/密钥由随机生成器产出，强度不低于 24 字符随机串，存于本机 `~/.config/kelp/`（不入库） |

端口规划：`11010` TCP+UDP（主链路，必须放行）；`11011` WS / `11012` WSS / `11013` WireGuard 按需，默认不开。

## 8. 里程碑与验收标准

**M0 勘测与骨架**
- 建立目录结构（`docs/`、`scripts/`、`config/`）、`git init`（main 分支）、提交任务书 + checkpoint + AGENT.md 引用
- VPS 只读勘测：系统版本、CPU/内存/磁盘、已装服务、`sysctl` 转发状态、安全组现状（**此步需用户授权后执行**）
- 产出 `docs/00-environment.md`（含两站点与 VPS 的实测基线：延迟、带宽、NAT 类型探测结果）
- 验收：git 历史干净（`main` 有初始提交）；勘测报告含可复现命令与原始输出；未对任何既有服务产生变更（附变更前状态记录）

**M1 公网节点上线**
- VPS 安装 EasyTier、写入 systemd 单元、配置私有模式与固定 IP；安全组放行 11010 TCP+UDP
- 验收：`systemctl status easytier` 常驻；`ss -lnup | grep 11010` 有监听；从家宽 `easytier-cli peer` 能看到 VPS 节点；**重启 VPS 后自动恢复**（`systemctl restart` + 真实 reboot 各一次）；**FR9 三条可视化路径至少跑通一条并定稿**（留档输出/截图与选型结论）

**M2 两站点组网 + 子网代理**
- 50.9 与 B 站点节点接入并代理各自网段；跨站点互访验证
- 验收（必须全部有原始输出留档 `docs/M2-acceptance.md`）：
  1. A→B：`ping 192.168.1.99`、`curl http://192.168.1.99:8000`（NAS 面板）、SSH 登录 B 站点设备
  2. B→A：反向同上
  3. **路径证明**：`easytier-cli peer` 显示与对端为 `p2p`（非 relay）；附丢包/延迟数据
  4. **速率基线**：`iperf3`（或 scp 大文件）分别测 P2P 与中继两条路径，写入文档（用于判断"何时开始花钱"）
  5. 网段无冲突确认（50 段 vs 1 段互不重叠）
  6. **面板覆盖验证**：全部节点在选定面板（FR9）中出现，含组网 IP 与在线状态；人为断开一个节点，验证"离线"判定与最后在线时间（路径 C）或面板状态变化

**M3 手机接入**
- Android 原生 App 加入网络；iOS 通过 WireGuard 门户配置接入
- 验收：蜂窝数据（关闭 Wi-Fi）下访问两站点的 NAS 面板与 SSH；`easytier-cli peer` 可见移动端节点；记录实际速率与路径（P2P/中继）

**M4 网关模式 + 收尾**
- 50.9 开启 `ip_forward` + FORWARD 放行（记录原值）；交付免客户端设备配置说明
- 验收：
  1. 一台**未装客户端**的设备（电视/AppleTV 或临时改网关的笔电）把网关指向 50.9 后，能访问 B 站点资源
  2. iOS/AppleTV 的 DNS 坑写入交付文档并被实际验证（至少 AppleTV 或 iOS 一台）
  3. 流量告警已配置并触发一次测试告警
  4. **回滚演练**：按 `docs/rollback.md` 停服务 → 确认本机与两站点网络恢复原状 → 重新启用成功
  5. **副作用专项验收（用户最关心）**：配置前后各做一次公网基线（`speedtest`/ping 常用站点），证明**公网访问路径与速率不受影响**；被改 DNS 的设备实测公网正常（打开常见网站 + 公司内网域名各一例）
  6. `docs/M4-acceptance.md` 归档全部证据

每完成一个里程碑：更新 `checkpoint.md`（状态表 + 日志追加），验收证据（命令输出）留档于 `docs/`。

## 9. 风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| P2P 打洞失败，流量长期走中继 | **真金白银**：0.8 元/GB | FR7 流量告警 + M2 速率基线 + 文档判据；必要时排查 NAT/UDP/防火墙 |
| 阿里云安全组未放行 / 放行过宽 | 节点不可用 / 暴露面过大 | 6.3 最小暴露；M1 逐端口核验 |
| 家宽 UDP 被 QoS 或 NAT 类型差 | 直连失败率高 | 保留中继兜底；记录 NAT 类型实测；必要时启用 KCP/QUIC 代理（v1 不做，视数据再评估） |
| B 站点（NAS 侧）环境未知、可能无 Docker/无 root | M2 阻塞 | M0 勘测先行；备选：该侧用一台常开 Linux/迷你主机做节点 |
| 与节点小宝并存冲突（虚拟网卡/路由） | 一方不通 | 两者 utun 独立；M2 验收时**同时开启**验证互不干扰；如冲突，明确优先级并记录决策 |
| VPS 单点故障 | 全部组网中断 | 组网本身可 P2P，节点只是信令；文档写明"节点宕机时已建立的连接仍可用"的实测结论 |
| 凭据泄露 | 服务器被入侵 | 6.6；建议 M1 完成后由用户评估是否改为密钥登录并关闭口令登录 |
| 用户既有铁律被违反（kill/卸载本地推理服务） | 会话与本地服务双失 | 6.2 写入铁律；任何 strace/kill/launchctl 操作前先自查 AGENTS.md 清单 |
| 改 DNS / 默认网关导致公网访问异常 | 设备上网受影响 | FR3 优先序 + 铁律 6.7 + M4 专项验收；已知例外：公司内网域名、路由器本地主机名、路由器 DNS 过滤 |
| Web 控制台选官方托管会暴露拓扑/配置 | 隐私风险 | 默认选自建（50.9，仅 overlay 可达）；若选官方托管须用户明确确认 |
| 面板"在线"判定误导 | 误判设备状态 | 明确"去中心化无名册"限制 + 期望名册比对（FR9） |

## 10. 决策记录

- D1（2026-09-19，用户）：项目名 **海带 / Kelp**；目录 `/Users/winann/01-project/07-kelp`。
- D2（2026-09-19，用户）：公网节点使用 **47.116.73.216**（阿里云上海）。
- D3（2026-09-19，用户）：ECS **按流量计费** → 节点只做信令与兜底，**禁止作为主力通道**（对应 FR7）。
- D4（2026-09-19，用户）：`winann.com` **未备案** → **不做公网域名 Web 入口**，全部走组网。
- D5（2026-09-19，用户）：功能精简，删除非必要项（见 §4.2）。
- D6（2026-09-19，Agent 提案，待用户随本任务书一并确认）：技术选型 **EasyTier**（Apache-2.0，国产、中文文档、支持子网代理/网对网/WireGuard 接入/私有公共节点，一个二进制即覆盖全部需求）；不选 Tailscale/Headscale（控制面境外、iOS 自定义控制服务器受限）、ZeroTier（根服务器境外、打洞一般）、frp/nps（只做端口转发，无组网）、NetBird（组件重）。
- D7（2026-09-19，用户）：**未经明确指令不得动手**（铁律 6.1），本项目先出任务书交审。
- D8（2026-09-19，Agent）：明确本机与节点小宝**并存**，不卸载节点小宝（用户仍在用），冲突时以实测数据决策（§9）。
- D9（2026-09-19，用户提问 / Agent 落实）：**统一设备查看界面列为 v1 功能**（FR9），三条路径（自建 Web 控制台 / 官方托管控制台 / 自建只读面板），M1 实测后定稿；默认倾向自建且不开放公网端口。
- D10（2026-09-19，用户提问 / Agent 落实）：**客户端接入以静态路由为先，不改默认网关**（铁律 6.7）；**DNS 默认不改**，仅在 iOS/AppleTV 实测受阻时按设备单独调整——目标是让"非节点设备的公网访问"零影响。
- D11（2026-09-19，用户 / Agent）：服务器凭据仅会话内使用、不入库（呼应铁律 6.6）；本次提供的 root 口令按此处理，已验证仓库内无残留。

## 11. 附录：2026-09-19 实测原始数据

- 节点小宝进程：`nblink`(PID 21874, 127.0.0.1:23020)、`NodeBabyLinkBackup`(6361)、`NodeBabyLinkRfile`(1680/1643)、`VPNPacket.appex`；路由表含 `10.222.222.1 utun7`、`100.66.1.1/.3/.4 via utun7`；对端 ping 全通。
- 47.116.73.216：ping 19.582/19.703/19.823 ms；端口 22 开放，80/443/8000/8080/8443/9000/11010/51820/3389/5900 全部 closed；SSH banner `SSH-2.0-OpenSSH_10.2p1 Ubuntu-2ubuntu3.6`；ipinfo：Shanghai / AS37963 Alibaba。
- 家宽：`curl https://api.ipify.org` → `36.151.107.250`；`traceroute -n -m 6 223.5.5.5` → 1) 192.168.50.1 2) **36.151.107.249** 3) * 4) 183.207.67.9 <br> 判定：WAN 网关与公网 IP 相邻（/30），非 CGNAT。
- IPv6：`ifconfig | grep inet6` 仅 `::1`；50.9 `ip -6 addr show scope global` 为空。
- 本机未安装：tailscale / zerotier / frp / nps / cloudflared / wireguard / easytier / colima。
- 站点可达性：192.168.50.1 up、192.168.50.9 up、192.168.50.11 up；**192.168.1.1 / 192.168.1.99 down**（无路由，`~/nas-nfs` 未挂载）。
- 爱快登录：`POST /Action/login {"username":"admin","passwd":"Abc@123456"}` → `{"Result":10001,"ErrMsg":"账号或密码错误"}`。
- 未备案拦截依据（检索）：境内云厂商对未备案域名实施网络层拦截，HTTP 80 返回备案提示页，HTTPS 443 通过 SNI 明文检测直接 RST；2025 年起部分厂商扩大到非标端口。
- ECS 计费依据（阿里云官方文档）：公网带宽"按使用流量"计费，出网 0.8 元/GB（入网不计费）；"按固定带宽" 10Mbps 约 525 元/月。
- EasyTier 可视化能力依据（官方文档，2026-09-19 查阅）：Web 控制台分两版——`easytier-web`（仅 API 后端）、`easytier-web-embed`（前后端一体）；默认 `11211/TCP`（前端+API 同端口），配置下发端 `22020/UDP`；Docker 镜像 `easytier/easytier:latest`，数据落 `/app` 下 `et.db`。节点以 `-w <用户名>` 启动才会被纳管；官方 FAQ 明确"以其他方式启动的机器不会出现在面板"，本机查看用 `easytier-cli node` / `easytier-cli peer`。
- `easytier-cli peer` 文档样例字段（v2.6.x）：`ipv4 | hostname | cost | lat_ms | loss_rate | rx_bytes | tx_bytes | tunnel_proto | nat_type | id | version`；其中 `cost` 为 `Local`/`p2p`/`relay`（判定是否走中继的依据）。
