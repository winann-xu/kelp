# 海带 (Kelp) 检查点 (Checkpoint)

用途：跨会话 / 跨 Agent 断档交接锚点。
规则：
1. 任何 Agent 接手本项目，先读 `项目任务书.md`（唯一权威任务书），再读本文件，然后从"下一步"继续。
2. 每完成一个里程碑（M0~M4）或做出任何新决策，立即更新本文件：更新"当前状态"与"里程碑状态表"，并在"日志"追加一条（日期 + 做了什么 + 验收证据位置）。
3. **未获用户明确"开始"指令前，禁止对任何目标机器执行变更操作**（任务书铁律 §6.1）。

## 当前状态

- 阶段：**M0 ✅ / M1 ✅ / M2 ✅ / M4 ✅；M3 机器侧全部完成 ✅，仅剩"用户手机真机测试"**（安全组已开通并已由外部真客户端实证可达）
- M4 已完成：流量护栏（自建，含测试告警证据）✅ / 回滚演练（3 场景含恢复）✅ / 公网副作用基线 ✅ / 面板 HTTPS+口令轮换 ✅
- M4 状态：**网关模式 ✅ 通过**（50.9 上前后对照：开启前全不通 → 开启后小包/大包/TCP/HTTP 200）；回滚演练 ✅（含网关模式 rollback + 重建）；流量护栏 ✅；公网基线 ✅
  - M4 待办（随 M3）：iOS/AppleTV 的 DNS/私密代理真机验证；事后公网基线复采（已完成，见 M4 §5）
  - **M3 进度（2026-09-20 00:2x）**：公网节点已开 WireGuard 门户（11013/udp）；① 本地自测通过（经隧道访问 NAS 200 / 家里 401）；② **外部实证通过**——在 50.9 用网络命名空间跑真 wg 客户端，握手 `Now`、NAS 200（飞牛 fnOS）、A 站点 401、隧道 12.7KB/4.2KB、宿主路由零扰动、残留清零（脚本 `scripts/diag-wg-portal-external.sh`）；③ 面板新增「移动端接入」区块；④ 新增**客户端下载页** `https://47.116.73.216:18080/dl/`（公开，143MB 七个平台包 + SHA-256 + 断点续传）与私有区 `/wg/conf`、`/wg/qr.png`（需登录）
    - 待用户（唯一剩余步）：手机关 Wi-Fi 用蜂窝数据，装 WireGuard App 扫 `~/kelp-run/wg-phone-qr.png` → 实测 `https://192.168.1.99:5667` 与 `https://192.168.50.9` → 回报
  - **M3 补记（2026-09-20 00:4x）**：① 用户报"电脑浏览器打不开面板" → 用 **WKWebView（Safari 引擎、不做证书豁免）复现超时 -1001**，同一工具加载 baidu 正常 ⇒ 根因是**证书信任链**（`curl -k` 会把这类问题完全遮住）；② 重签证书（补 SAN/EKU/127.0.0.1/localhost，旧证书备份 `panel.{crt,key}.bak.20260920003424`）+ 装进用户 Mac 钥匙串信任 ⇒ **WebKit 复测成功加载**；③ 证书公开到 `/dl/kelp-panel.crt` 供其他设备导入；④ **面板新增「客户端下载」入口**（顶栏 chip + 区块内各平台直链 + WireGuard 配置/二维码，用户要求）；⑤ 实测公网节点出网带宽 **1–3.5 Mbps**（50.9 下 10.4MB 用 86s、Mac 用 25s）——这才是"手机慢"的根源，面板页面仅 9.6KB。
  - 未完成/待跟进：给 50.9 的日志抓包因 ssh 断开被带走过一次（nohup 未真正脱离会话）→ 以后远端抓包用 `systemd-run` 或 `setsid`，别用 `nohup ... &`。
    - ⚠️ 计费：WG 门户流量经公网节点转发 = 0.8 元/GB（大流量改用原生客户端，Android 免费/iOS 需外区 Apple ID）→ D26
  - 50.9 上的 **sudo 已临时开放给 winann 免密**（用户执行 A 方案，文件 `/etc/sudoers.d/kelp-agent`）→ 项目收尾时应提示用户删除
- **网关模式已定位根因并修好（2026-09-19 23:3x）**：首轮自测 ping 通但 HTTPS 不通 → 在公网节点用命名空间逐层复现 → 确认必须 SNAT（转发源为私网地址，对端无回程路由）；MSS 钳制/校验和填充均非根因（MSS 作为防御保留）。**判据更正：不能用 ping 判断转发是否可用，必须用 TCP。**（D24）
- 更新时间：2026-09-19 22:5x
- 下一步：① **M3 手机接入**（Android 原生 App / iOS WireGuard 门户），含手机端速率与路径验收；
  ② M4 网关模式（不装客户端设备）+ 阿里云流量告警 + 回滚演练；
  ③ 顺带：50.9 与 hub 补 `--need-p2p`、50.9 的 RPC 白名单补 `127.0.0.1`（同一次 sudo 窗口）
- 面板已上线：`https://47.116.73.216:18080`（自签证书；用户名/口令见 `~/.config/kelp/kelp.env`，不入库）

## 里程碑状态表

| 里程碑 | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| M0 | 勘测与骨架（目录/git、VPS 与两站点基线勘测报告） | ✅ 完成（2026-09-19） | docs/00-environment.md（VPS 规格、安全组实测、Stash TUN 假阳性取证、延迟/带宽基线、安装包投递链） |
| M1 | 公网节点上线（EasyTier 私有节点 + systemd + 安全组 11010） | ✅ 完成（2026-09-19 22:3x） | docs/M1-acceptance.md（hub 常驻 + 50.9 接入 + P2P 直连 16ms + 子网代理穿透 LAN + FR9 面板上线） |
| M2 | 两站点组网 + 子网代理（含 P2P 路径证明与速率基线） | ✅ 完成（2026-09-19 22:5x） | docs/M2-acceptance.md（三节点全 P2P；双向穿透（含 NAS→家里服务 401@0.11s）；跨站点由 relay(2)/30ms 修正为 p2p/udp/21.5ms；速率基线 17.6–18.1 Mbps；面板 3 台全在线） |
| M3 | 手机接入（Android App / iOS WireGuard 门户）+ 客户端分发页 | 🔄 机器侧 ✅ 完成（2026-09-20 00:2x），仅剩用户真机测试 | docs/M3-acceptance.md（门户部署、本地+外部双重实证、下载页、校验值与续传实测、证据图 docs/evidence/M3-download-page*.png） |
| M4 | 网关模式 + 收尾（流量告警、回滚演练） | ✅ 完成（2026-09-19 23:3x） | docs/M4-acceptance.md（网关前后对照、SNAT 根因、回滚演练、流量护栏告警证据、公网节点硬单点实测） |

## 已确认的关键决策（详见任务书 §10）

- 项目名：海带 / Kelp；目录：/Users/winann/01-project/07-kelp（D1）
- 公网节点：47.116.73.216（阿里云上海，root）（D2）
- ECS 按流量计费 0.8 元/GB → 节点只做信令与兜底，禁止当主力通道（D3，FR7）
- winann.com 未备案 → 不做公网域名 Web 入口，全部走组网（D4）
- 功能精简：删除 Cloudflare/DDNS/RustDesk 中继/Immich/ACL/门户页等非必要项（D5，范围见 §4.2）
- 技术选型建议 EasyTier（D6，待用户确认）
- 未经明确指令不得动手（D7，铁律 §6.1）
- 与节点小宝并存，不卸载（D8）
- 统一设备查看界面列为 v1 功能（FR9）：自建 Web 控制台 / 官方托管控制台 / 自建只读面板三选一，M1 实测定稿，默认自建且不开公网端口（D9）
- 客户端接入优先静态路由，**不改默认网关**；**DNS 默认不改**（D10，铁律 6.7）
- 凭据仅会话内使用、不入库（D11；已验证仓库无残留）
- A 站点节点落 **50.9**，Mac 节点后置（D12；实测 Stash TUN 接管路由导致 Mac 连 hub 握手超时）
- 安装包投递：ghfast 镜像 → VPS/Mac → LAN scp，SHA-256 三处核对（D13；VPS 直连 GitHub 超时、50.9 连镜像 000）
- FR9 面板端口预留 **18080/TCP**，因未备案走 `http://IP:18080` + 强口令（D14）
- 外网连通性结论只采信 50.9 或不绑接口时的假阴性规避做法（D15）
- 子网代理**不需要**开启 50.9 的 `ip_forward`、也不需要 iptables NAT（实测 EasyTier 用户态转发，铁律 6.4 未被触发）（D16）
- FR9 面板已按路径 C 上线（`https://47.116.73.216:18080`，Python 标准库实现 + systemd，**TLS 已启用**、只读、Basic 认证、中继预警）（D17/D20）
- B 站点落 **飞牛 NAS（Docker 容器）**：fnOS SSH 48032，admin 属 docker 组免 sudo；镜像走 `docker.1ms.run` 前缀；参数含 `--need-p2p true`（D18）
- **跨站点若出现 relay 立即处置**：`--need-p2p` 实测把 `relay(2)/30ms` 升级为 `p2p/udp/21.5ms`，避免 0.8 元/GB 计费（D19）

## 待验证：跨站点 P2P 回落中继（2026-09-19 23:3x 现象）

公网节点反复重启后，NAS ↔ 50.9 从 `p2p/udp/21.5ms` 回落为 `relay(2)/31ms`（**当前是 relay 状态，但 0 B 流量通过，尚未产生费用**）。已试过的处置与结果：

| 处置 | 结果 |
|---|---|
| 等 90 秒 | 未自动升级 |
| 重启 B 站点节点（NAS 容器，带 `--need-p2p`） | 仍未升级 |
| 触发跨站点流量（3 次 curl） | 未触发打洞（lazy-p2p 未生效） |
| hub 加 `--relay-all-peer-rpc true` | 单独未生效（保留该参数） |

**下一步**：用户执行 `/tmp/kelp-finish-50-9.sh` 时会给 A 站点节点补上 `--need-p2p true` 并重启 → 预期重新打洞成功。若仍为 relay，按《排查顺序》升级处置（详见 docs/M4-acceptance.md §7）。

**排查顺序**（面板红色横幅出现时照此执行）：①看面板/`easytier-cli peer` 的 cost ②触发一次跨站点流量 ③重启任一侧节点服务 ④确认双侧均有 `--need-p2p` ⑤兜底：接受中继但盯流量告警（0.8 元/GB）。

## 待办 / 阻塞

- [x] 安全组放行（用户已做）：11010 TCP+UDP、18080 TCP；11010 复测连通 ✅
- [x] 50.9 节点安装完成（用户执行；`--peers` 参数笔误已修）→ 服务 active、P2P 直连成立 ✅
- [ ] M1 收尾：安全组放行后复测双向可见 + `systemctl restart` 与真实 reboot 各一次验证自启
- [x] **B 站点已接入**（2026-09-19 22:5x）：用户开放 fnOS SSH(48032) → Docker 部署 `kelp-b-node`，代理 192.168.1.0/24，与两站点 P2P 直连。原记录如下（存档）：
- [-] ~~B 站点接入被"入口"卡住~~（2026-09-19 22:4x 实测）：
      ① 用户提供：NAS（192.168.1.0/24 段内）经节点小宝可达，mesh IP `100.66.1.7`，账号 admin，**有 Docker**；
      ② 实测：100.66.1.7 的 **HTTPS/HTTP 可用**（`https://100.66.1.7:5667` = 飞牛 fnOS 登录页，RTT 0.63ms），
         但**非 HTTP 通道被掐**——`ssh 100.66.1.7:22/222/8022` 全部 `Connection closed`（无 banner），
         且任意高端口（45678）也显示"可连"，说明 Mac 侧节点小宝客户端对 100.66.x.x 做了 TCP 接管，只放行 HTTP(S)；
      ③ 本地网段（192.168.50.0/24）扫描 5667/8000/22/443 共 254 台：**未发现 NAS 的直连地址**
         （命中 .53/.188 等 OpenSSH 主机与 .70/.53/.188 的 8000 端口，逐个取标题均非 fnOS）；
      ④ 结论：要在 NAS 上落 B 站点节点，需要一个入口 —— 待用户选择：(A) 在 fnOS 开启 SSH（推荐，之后全自动）；
         (B) 用 fnOS 的 Docker 界面按给定参数新建容器；或 (C) 暂缓 B 站点，先做 M3 手机接入。
- [ ] 待确认：是否为该项目创建 GitHub 远端仓库（AGENT.md 要求项目一开始就配置 git + GitHub）；本地 `git init` 已完成，远端未建（另需确认 public/private）
- [ ] 待确认：B 站点（NAS 侧，192.168.1.0/24）具体由哪台设备做组网节点，该设备是否有 Docker/root
- [ ] 待提供（部署时交互输入，不入库）：47.116.73.216 root 凭据——用户已口头提供，禁止写入仓库/文档/日志
- [ ] 环境核实（M0 内执行，需授权）：VPS 系统版本/资源/已装服务/sysctl/安全组现状
- [ ] 待用户选 FR9 可视化路径（默认 A 自建 Web 控制台部署在 50.9；或 C 只读面板）；M1 实测定稿

## 日志

- 2026-09-20 00:2x **M3 收尾（机器侧）+ 客户端分发页**：① 用户开通安全组 `11013/UDP`；② 为免用户"首次试错"，在 **50.9 用网络命名空间**跑真 WireGuard 客户端做外部实证（`scripts/diag-wg-portal-external.sh`）：握手 `Now`、NAS 200（飞牛 fnOS）、A 站点 401、隧道 12.7KB/4.2KB、宿主默认路由未变、netns/veth/规则/工具包全部清理（临时 `dpkg -i` 装 wireguard-tools 后 `dpkg -P` 卸载）；③ **客户端下载页上线**：`https://47.116.73.216:18080/dl/`（公开，面板本体仍鉴权）托管 Windows/macOS/Android/Linux 共 7 个 EasyTier v2.6.4 官方包（143MB，ghfast 取回），含 SHA-256 校验、断点续传（206 实测）、页面内自带二维码；手机 WireGuard 配置移到鉴权区 `/wg/conf`、`/wg/qr.png`；④ 修两个面板 bug：`human_size()` 非 B 单位多除一次 1024（显示 0.0 MB）、下载接口不支持 Range/HEAD；⑤ 发现并修复**助手自己写的凭据泄漏**：曾把手机 WG 私钥写进 `scripts/diag-wg-portal.sh`（已改为从文件读、重写该次提交并清历史，全仓库复检零命中）；⑥ 附带实证：50.9 的 `apt` 同样被死代理 `192.168.50.12:10809` 挡死（`curl --noproxy '*'` + `dpkg -i` 绕过）。
- 2026-09-20 00:1x WireGuard 官方客户端安装包**无法托管**（实测 `download.wireguard.com`、`f-droid.org` 从国内/本机均不可达，GitHub 无官方 Release 资产）→ 下载页只给商店/官网链接，并明确"Windows/macOS/Android 优先用 EasyTier 原生客户端（P2P 不计费），WireGuard 实际只为 iOS 服务"（D27）。

- 2026-09-19 立项（用户指令）：完成节点小宝替代方案调研 v1→v3（文档存于 /Users/winann/04-AI调研报告/，含实测数据与横向对比）；用户拍板项目名（海带/Kelp）、目录、公网节点、两条约束（按流量计费、未备案）与精简指令；创建本任务书（v1.0）与本检查点；`git init`（main）。**未对任何目标机器执行变更操作**（用户要求先审任务书）。
- 2026-09-19 22:3x **M1 收口 + 面板上线**（用户放行安全组、在 50.9 执行安装脚本后）：① 安全组复测 tcp/11010 从 50.9 0.02s CONNECTED；② 首次安装失败（退出码 2）定位为 `--peer` 应为 `--peers`，修正并以"普通用户预检参数"法验证后由用户重跑，服务 active；③ 组网成立：hub 视角 `10.144.144.9 cost=p2p lat=16.43ms loss=0.0% tunnel=tcp`，overlay ping 16.6ms；④ **子网代理穿透实测**：从上海 hub 直接 ping 通家里 192.168.50.1/.10/.9，`https://192.168.50.9/` 返回 401（链路真实可用），且 50.9 的 `ip_forward` 仍为 0 → 结论：无需动系统转发与 iptables；⑤ **FR9 只读面板上线**：`/opt/kelp/panel.py` + `panel_ui.html` + `kelp-panel.service`（Python 标准库、Basic 认证、5s 轮询各节点 RPC、离线名册比对、中继预警）；本机与家宽双端验证 200/401 与无密钥泄漏；用离屏 WKWebView 截图 + 视觉检查复核桌面/手机两版 UI 并修掉三处排版瑕疵；⑥ 任务书与验收文档同步（D16/D17）。
- 2026-09-19 M0 完成 + M1 启动（用户指令"开始项目"）：① VPS 只读勘测（Ubuntu 24.04.4 / 2C1.7G / 40G / 无 docker / iptables 全 ACCEPT，端口过滤在安全组）→ docs/00-environment.md；② 装 EasyTier 2.6.4（ghfast 镜像，VPS 直连 GitHub 超时）为 hub：`/usr/local/bin/easytier-{core,cli}` + `/etc/systemd/system/easytier.service`（EnvironmentFile 注入密钥、`LimitNOFILE=1048576`、`Restart=always`），`private_mode=true`、组网 IP 10.144.144.1、监听 tcp/udp 11010、RPC 仅 127.0.0.1:15888、TUN 就绪；③ 连通性实测发现**安全组只放行 22**（11010 TCP 从 50.9 超时、UDP 五端口 tcpdump 零包）→ M1 阻塞；④ 修正此前误判：Mac 上 `nc` 显示 11010 "OPEN" 是**假阳性**，`route get 47.116.73.216` 走 `utun6`（Stash TUN 模式），绑定 en0 后真实结果为超时；⑤ 产出 `scripts/setup-node.sh`（幂等节点安装器）并把二进制（SHA-256 `61b659ea…dfd6` 三处一致）+ 身份文件 + 一键包装脚本投递到 50.9 `/tmp`（600）；⑥ 任务书新增 D12–D15 与风险行。
- 2026-09-19 用户提出两项确认（① 是否有统一界面查看设备/IP/在线离线；② 非节点设备改 DNS 是否影响公网访问）。Agent 查阅 EasyTier 官方文档（Web 控制台部署参数、FAQ 纳管限制、`easytier-cli` 字段）后答复，并更新任务书至 v1.1：新增 **FR9**（可视化三路径 + "去中心化无名册"限制说明 + 名册比对实现离线判定）、重写 **FR3**（静态路由优先 / DNS 默认不改 / 改默认网关为次选）、新增铁律 **6.7**、M1/M2/M4 验收各补一项（含 M4 公网基线对比专项）、风险表 3 行、决策 **D9–D11**。仍未对任何目标机器做变更。
